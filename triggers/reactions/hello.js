const Discord = require("discord.js");
module.exports = {
	name: ["hello", "say hi", "say-hi", "xin chào"],

	execute(message, args) {
		// message.react("👋");
		// message.reply({
		// 	content: `${Discord.Util.cleanCodeBlockContent(message.content)}\n${Discord.Util.cleanContent(message.content,message.channel)}`,
		// });
	},
};
